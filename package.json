{
  "name": "rainbows-app",
  "version": "3.18.4",
  "private": true,
  "type": "module",
  "scripts": {
    "check:syntax": "node --check app.js && node --check rainbows-overrides.js && node --check sw.js",
    "test": "npm run check:syntax && node --test tests/*.test.mjs"
  }
}
